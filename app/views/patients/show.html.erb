<div class="container">
  <br>
  <h4 style="font-family: 'Roboto Slab', serif; color: #007bff" >Hello <span style="color: #ff5b62;"><%= @current_patient.first_name %>!</span></h4>
  <br><br>
  <div class="row">
    <div class="col-md-4">
      <p>Create, view and update doctor appointments, searching by specialty and insurance.</p>
      <p><%= button_to "Schedule an appointment", search_path(), class:"btn btn-primary", :method => :get %></p>
    </div>
    <div class="col-md-4">
      <p>Manage your doctor appointments using your preferred calendar view: Day, Three Day, Week or Month.</p>
      <p><%= button_to "See Appointments", appointments_path(), class:"btn btn-primary", :method => :get %></p>
    </div>
    <div class="col-md-4">
      <p>Change your user's informaation and update your password.</p>
      <p><%= button_to "Edit Account", edit_patient_path(@current_patient), class:"btn btn-primary", :method => :get %></p>
    </div>
  </div>











  <br><br><br><br>
  <%= button_to "Delete Account", patient_path(@current_patient), :method => :delete, class:"btn btn-danger", title: 'Delete', id: 'delete_btn', data: { confirm: 'Are you sure?', disable_with: 'loading...' } %>
  <%# <%= button_tag 'Delete', onclick: patient_path(@current_patient), :method => :delete, class: 'btn btn-danger', title: 'Delete', id: 'delete_btn', data: { confirm: 'Are you sure you want to delete this record?' } %>

</div>
